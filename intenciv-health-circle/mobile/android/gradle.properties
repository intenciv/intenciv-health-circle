# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Automatically convert third-party libraries to use AndroidX
android.enableJetifier=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=true

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Enable GIF support in React Native images (~200 B increase)
expo.gif.enabled=true
# Enable webp support in React Native images (~85 KB increase)
expo.webp.enabled=true
# Enable animated webp support (~3.4 MB increase)
# Disabled by default because iOS doesn't support animated webp
expo.webp.animated=false

# Enable network inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Use legacy packaging to compress native libraries in the resulting APK.
# true = defense-in-depth for 16KB page-size compliance: .so files are
# extracted at install time rather than mmap'd directly from the APK,
# which Google's own docs confirm is an independently valid compliance
# path regardless of the underlying .so alignment (kept alongside the
# NDK r28 fix in android/build.gradle, not instead of it).
expo.useLegacyPackaging=true

# Fixes a real startup crash confirmed via live device testing:
# com.facebook.soloader.SoLoaderDSONotFoundError: couldn't find DSO to
# load: libreact_featureflagsjni.so
#
# Root cause: react-native-gradle-plugin (RNGP) has a built-in pickFirst
# list for resolving duplicate .so packaging conflicts (JNI-provided vs.
# prefab-provided copies of the same library) for libraries like
# libfbjni.so, libjsi.so, libreact_newarchdefaults.so, etc. - see RNGP's
# own TaskConfiguration.kt. libreact_featureflagsjni.so is a newer
# library RNGP's bundled pickFirst list was never updated to include.
# Without an explicit rule, AGP's default behavior when it finds
# duplicate copies of the same .so path from different sources is to
# silently drop the file - explaining exactly this crash (a real, still-
# open upstream issue: github.com/facebook/react-native/issues/50144,
# github.com/expo/expo/issues/40359).
android.packagingOptions.pickFirsts=**/libreact_featureflagsjni.so
